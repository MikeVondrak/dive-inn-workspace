import { AnimateViewportOverlayDirective } from './animate-viewport-overlay.directive';

describe('AnimateViewportOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateViewportOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
