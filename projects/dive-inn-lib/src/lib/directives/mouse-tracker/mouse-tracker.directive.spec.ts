import { MouseTrackerDirective } from './mouse-tracker.directive';

describe('MouseTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
