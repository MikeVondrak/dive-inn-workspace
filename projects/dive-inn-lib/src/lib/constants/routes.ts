
export const routes = {
  api: {
    _root: '/api',
    reservation: {
      _root: '/reservation'
    },
    specials: {
      _root: '/specials'
    },
    events: {
      _root: '/events'
    }
  }
}